import logo from "../img/Voxels Logo.png"

export default function () {
  return (
    <header>
      <img src={logo} />

      <a href="#" className="connect-wallet">
        <p>CONNECT WALLET</p>
      </a>
    </header>
  )
}
